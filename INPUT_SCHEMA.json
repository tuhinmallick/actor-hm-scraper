{
    "title": "Enhanced HM.com Actor - Best Practices Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "inputCountry": {
            "title": "Country",
            "type": "string",
            "description": "Select country to scrape from",
            "editor": "select",
            "default": "UNITED KINGDOM",
            "enum": ["UNITED KINGDOM", "USA", "ITALY", "GERMANY", "FRANCE", "AUSTRALIA", "SPAIN", "CANADA", "MEXICO"],
            "enumTitles": ["UNITED KINGDOM", "USA", "ITALY", "GERMANY", "FRANCE", "AUSTRALIA", "SPAIN", "CANADA", "MEXICO"]
        },
        "maxItems": {
            "title": "Max items",
            "type": "integer",
            "description": "Maximum number of products to save (leave empty for no cap)",
            "editor": "number",
            "minimum": 1,
            "maximum": 100000,
            "default": 1000
        },
        "maxRunSeconds": {
            "title": "Max run time (seconds)",
            "type": "integer",
            "description": "Maximum time to run the scraper in seconds",
            "editor": "number",
            "minimum": 60,
            "maximum": 86400,
            "default": 3600
        },
        "enableAntiBot": {
            "title": "Enable Anti-Bot Detection Evasion",
            "type": "boolean",
            "description": "Enable advanced anti-bot detection evasion techniques",
            "editor": "checkbox",
            "default": true
        },
        "enableProgressiveSaving": {
            "title": "Enable Progressive Data Saving",
            "type": "boolean",
            "description": "Save data as it's scraped to prevent data loss",
            "editor": "checkbox",
            "default": true
        },
        "batchSize": {
            "title": "Batch size",
            "type": "integer",
            "description": "Number of products to save in each batch",
            "editor": "number",
            "minimum": 10,
            "maximum": 500,
            "default": 50
        },
        "minQualityScore": {
            "title": "Minimum quality score",
            "type": "integer",
            "description": "Minimum quality score for products to be saved (0-100)",
            "editor": "number",
            "minimum": 0,
            "maximum": 100,
            "default": 70
        },
        "enableMemoryOptimization": {
            "title": "Enable Memory Optimization",
            "type": "boolean",
            "description": "Enable memory usage monitoring and optimization",
            "editor": "checkbox",
            "default": true
        },
        "debug": {
            "title": "Debug mode",
            "type": "boolean",
            "description": "Enable debug logging for troubleshooting",
            "editor": "checkbox",
            "default": false
        },
        "useMockRequests": {
            "title": "Use mock requests (testing only)",
            "type": "boolean",
            "description": "Use mock requests for local testing (DO NOT USE IN PRODUCTION)",
            "editor": "checkbox",
            "default": false
        }
    },
    "required": ["inputCountry"]
}
